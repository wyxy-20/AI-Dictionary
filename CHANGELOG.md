# Changelog

本项目遵循 [Semantic Versioning](https://semver.org/lang/zh-CN/)。
所有可感知的变更都会记录在本文件中。

## [1.9.1] - 2026-08-16

### 修复
- **词库同步失败**：远程 terms.json（约 450KB）经国内网络通道下载缓慢，
  原 8 秒超时必然失败（只有内置 319 条）。本次：
  - **完整词库（1125 条）随安装包内置**，新装用户启动即拥有完整词库，
    无需联网等待；
  - 本地词库版本与远程对齐后，启动跳过下载（仅远程更新时才增量同步）；
  - 远程请求超时 8 秒 -> 30 秒，为增量下载留足余量。

### 工程
- 新增 `tool/sync_seed.ps1`：发版前同步远程词库到内置种子数据

## [1.9.0] - 2026-08-16

### 新增
- 远程词库多源回退：jsDelivr CDN（主）→ GitHub Raw（备），国内网络可达性更好，
  version.json 与 terms.json 强制同源，避免 CDN 缓存错配
- 自动备份：每 7 天备份一次数据库到应用支持目录（保留最近 5 份）
- 运行日志系统：写入 `%APPDATA%/com.aidictionary/AI Dictionary/logs/app.log`，
  512KB 自动轮转，设置页可一键复制日志用于反馈
- 设置页新增"检查更新"（查询 GitHub 最新 Release）与"反馈问题"（复制 Issue 链接）入口
- AI 服务设置区补充 API Key DPAPI 加密说明（重装系统需重新填写）
- 顶部搜索框增加 200ms 防抖，词库扩容后输入更流畅
- 数据导出支持注入备份目录（便于测试与自定义路径）

### 修复
- 版本号统一由 `test/version_consistency_test.dart` 校验，
  `pubspec.yaml` 与 `app_config.dart` 不再允许漂移
- README 中失效的 `AI-Terms-Database/` 相对链接改为指向独立仓库
- README 同步源/版本基线等陈旧描述更新为多源与当前版本
- 测试环境启动挂起：日志与备份不再依赖 path_provider 平台通道

### 工程
- CI 新增 Windows 平台构建 job（`flutter build windows --release`）
- 新增词库质量校验脚本 `tool/validate_terms.py`（字段完整性 / 重复检查 / 难度范围）
- 新增 `.fvmrc` 锁定 Flutter 3.44.8
- 新增测试：日志、自动备份、数据导出、更新检查、多源回退、版本一致性（共 89 个用例）
- 新增 Inno Setup 安装包（`tool/setup.iss` + `build_windows.ps1` 自动生成）：
  标准安装/卸载流程，控制面板「程序和功能」可卸载，卸载时询问是否同时删除用户数据

## [1.8.1] - 2026-08-11

### 变更
- 应用图标去白底（透明圆角）并定版 v1.8.1

## [1.8.0] - 2026-08-11

### 新增
- API Key 使用 Windows DPAPI 加密存储（旧版明文自动迁移）
- 单实例 + 系统托盘（关闭窗口驻留后台，全局快捷键仍可用）

## [1.7.0] - 2026-08-11

### 新增
- 全局悬浮快捷搜索：自定义任意快捷键，在任意应用中调出置顶搜索窗

## [1.6.0] - 2026-08-11

### 新增
- 全局快捷搜索（设置页配置快捷键 + 弹窗实时搜索）
- 界面语言本地化（简体中文 / English 即时切换）

## [1.5.0] - 2026-08-11

### 新增
- AI 智能解释系统：OpenAI 兼容接口 + SQLite 缓存（词条版本变更自动失效）
- AI 解释右侧侧边栏（显示 / 隐藏 / 复制 / 重新生成）
- AI 服务设置：测试连接、错误详情展示、endpoint 兼容拼接

## [1.0.0] - 2026-08-11

### 新增
- 初始版本：319 个内置 AI 术语、三栏式界面、实时搜索、A-Z 导航、
  收藏 / 浏览历史、设置（主题 / 语言 / 数据管理）、启动自动同步远程词库
